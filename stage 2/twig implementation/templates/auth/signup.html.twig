{# Replaces SignupPage.jsx #}
{% extends 'base.html.twig' %}

{% block title %}Sign Up{% endblock %}

{% block body %}
    <div class="container">
        <div class="auth-wrapper">
            <div class="card auth-card">
                <h1 class="auth-title">Create a new account</h1>
                <p class="auth-subtle">Create your account to get started.</p>

                <form action="{{ path('auth_signup') }}" method="POST">
                    <div class="form-row">
                        <label for="email" class="label">Email address</label>
                        <input
                            id="email"
                            name="email"
                            aria-label="Email address"
                            value="{{ old_email|default('') }}" {# Retains email on error #}
                            type="email"
                            required
                            class="input"
                        />
                    </div>

                    <div class="form-row">
                        <label for="password" class="label">Password</label>
                        <input
                            id="password"
                            name="password"
                            aria-label="Password"
                            type="password"
                            required
                            class="input"
                            minlength="6"
                        />
                    </div>

                    {% if error_message %}
                        <div class="auth-error">{{ error_message }}</div>
                    {% endif %}

                    <div class="form-row auth-submit-row">
                        <button type="submit" class="button button--primary">
                            Sign up
                        </button>
                    </div>
                </form>

                <p class="auth-bottom-text">
                    Already have an account? <a href="{{ path('auth_login') }}" class="auth-bottom-link">Sign in</a>
                </p>
            </div>
        </div>
    </div>
{% endblock %}
